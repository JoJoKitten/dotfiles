# This is the configuration file for GNU readline, which is used e.g. in bash 
# and the python interactive console.
# It is not used in zsh though.

# vi style editing
# does the same as "set -o vi" in bashrc
set editing-mode vi

# Tab to complete
TAB: menu-complete
# Shift-Tab to choose previous
"\e[Z": menu-complete-backward
# first press of Tab diplays choices
set menu-complete-display-prefix on
set show-all-if-ambiguous on

# various other settings
set colored-stats on
set colored-completion-prefix on
set completion-ignore-case on
set bell-style none
set echo-control-characters off

